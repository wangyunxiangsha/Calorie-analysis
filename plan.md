# 每日卡路里与营养分析 — 需求文档

> 最后更新：2026-05-17  
> 产品需求全文见 **[prd.md](./prd.md)**（PRD）；本文档侧重迭代计划与变更记录。  
> 形态：微信小程序 + 简单管理后台  
> 录入优先级：**拍照识别 > 手动选食物 > 扫码**  
> **技术选型（已确认）**：NestJS + TypeScript + PostgreSQL + Prisma · 微信原生小程序 · Vue3 + Element Plus 后台 · 食物库自建 800~1200 条（MVP 先种子数据 + CRUD）

---

## 1. 产品定位

一款面向多健康目标的**每日摄入分析**工具：用户通过拍照或选食物记录饮食，系统按所选**健康模式**汇总卡路里与营养素，并给出可执行的当日/周期反馈。

**不做（第一版）**：社区、电商、课程、公开排行榜、医疗诊断结论。

---

## 2. 健康模式（全人群、分模式）

用户在 onboarding 或设置中选择主模式，可随时切换；切换后**目标阈值与提示文案**变化，底层记录能力共用。

| 模式 ID | 名称 | 核心用户 | 目标侧重 | 关键营养素/指标 |
|---------|------|----------|----------|-----------------|
| `lose_fat` | 减脂 | 控重、外卖族 | 热量缺口、均衡宏量 | 卡、蛋白、碳水、脂肪、纤维 |
| `gain_muscle` | 增肌塑形 | 健身人群 | 蛋白达标、训练日差异 | 蛋白优先，碳水周期化 |
| `metabolic` | 代谢/慢病管理 | 控糖控钠等 | 限制项红线提示 | 糖、钠、嘌呤（可选）、饱和脂肪 |
| `pregnancy` | 孕产期 | 孕妇/产后 | 阶段化体重与微量营养 | 铁、钙、叶酸提示（非医疗建议） |
| `wellness` | 轻健康 | 自律、无硬目标 | 温和反馈、趋势向好 | 均衡摄入、少告警 |

### 2.1 各模式差异化规则（产品层）

- **减脂**：默认 TDEE − 300~500 kcal；超标黄色/红色；强调「今日剩余额度」。
- **增肌**：蛋白 g = 体重 kg × (1.6~2.2) 可配置；可选「训练日 / 休息日」碳水系数。
- **代谢管理**：钠、添加糖、碳水上限严格；单项超标即提示，支持导出给医生。
- **孕产期**：按 trimester / 产后周数切换推荐区间（文案需标注「仅供参考，遵医嘱」）。
- **轻健康**：默认宽目标；以周趋势「比上周好」为主反馈。

### 2.2 模式与 UI

- 首页仪表盘、提醒语气、推荐文案随模式变化。
- 历史数据不因切换模式而丢失；切换后**按新模式重新计算达标状态**。

---

## 3. 录入方式（优先级）

| 优先级 | 方式 | 第一版范围 | 说明 |
|--------|------|------------|------|
| P0 | **拍照识别** | MVP 必做 | 菜品图 → 识别品名 + 估份量 → 用户确认后入库 |
| P1 | **手动选食物** | MVP 必做 | 搜索/分类/常吃/最近；支持多餐、份数 |
| P2 | **扫码** | V1.1+ | 包装食品条形码 → 营养表；依赖商品库 |

**交互原则**：任何方式录入后都进入**确认页**（品名、份量、营养可微调），3 秒内可完成确认。

---

## 4. 第一版功能范围（MVP）

### 4.1 小程序端

**账号与资料**

- 微信登录
- 基础资料：性别、年龄、身高、体重、活动量
- 选择健康模式 + 目标体重（可选）

**饮食记录**

- 拍照识别（见 §5 技术方案）
- 手动选食物（食物库搜索、份量、餐次：早/午/晚/加餐）
- 当日汇总：热量、蛋白质、碳水、脂肪 + 模式相关指标
- 进度条：已摄入 / 目标、剩余额度

**目标与反馈**

- 按模式生成每日目标（可调）
- 单餐/单日：达标 · 注意 · 超标
- 7 日趋势折线（至少：热量 + 蛋白）

**我的**

- 模式切换、目标修改
- 隐私说明（数据用途、不公开）

### 4.2 管理后台（简单）

| 模块 | 功能 |
|------|------|
| 登录 | 管理员账号密码（单角色即可） |
| 食物库 | 食物 CRUD：名称、分类、每 100g 营养、常见份量 |
| 识别反馈 | 用户纠错记录列表（用于优化识别/库） |
| 用户概览 | 注册数、日活、人均记录次数（只读统计） |
| 模式配置 | 各模式默认目标系数（JSON 或表单，避免硬编码发版） |

**第一版后台不做**：订单、内容运营、复杂权限、营养师 IM。

---

## 5. 技术方案建议

### 5.1 整体架构

```
[微信小程序] ──HTTPS──> [API 服务] ──> [PostgreSQL / MySQL]
                              │
                              ├── 对象存储（餐图）
                              └── 视觉识别服务（拍照）
[管理后台 Web] ──HTTPS──> 同一套 API（/admin/*）
```

### 5.2 已确认技术栈

| 层级 | 选型 | 备注 |
|------|------|------|
| 小程序 | **微信原生** | 拍照主路径；目录 `apps/miniprogram` |
| API | **NestJS + TypeScript** | 目录 `apps/api` |
| ORM | **Prisma** | 迁移与种子数据 |
| 数据库 | **PostgreSQL 16** | 本地 Docker Compose |
| 后台 | **Vue3 + Vite + Element Plus** | 目录 `apps/admin` |
| 存储 | 腾讯云 COS / 阿里云 OSS（M2） | M0 可先本地/占位 URL |
| 识别 | **大模型视觉**（OpenAI 兼容 API）+ 食物库映射（M2） | 已接入；未配密钥时 mock |
| 食物库 | **自建 800~1200 条** | M0 种子 ~50 条；后台 CRUD 持续扩充 |

### 5.2.1 仓库结构

```
Calorie analysis/
├── apps/api/           # NestJS API
├── apps/admin/         # Vue 管理后台
├── apps/miniprogram/   # 微信原生小程序
├── docker-compose.yml
├── plan.md
├── standard.md
└── README.md
```

### 5.3 拍照识别流程

1. 小程序上传图片 → API → 对象存储  
2. 调用**菜品识别 API** → 返回 Top-N 候选菜名 + 置信度  
3. 服务端用**食物库 fuzzy match** 映射到标准食物 ID  
4. 默认份量（如 1 份/碗）预填，用户在确认页修改  
5. 低置信度：引导用户搜索手动选（P1 兜底）

**关键**：识别结果必须经用户确认，避免自动记账错误伤害信任。

### 5.4 核心数据模型（草案）

- `users`：微信 openid、资料、当前模式  
- `user_goals`：模式、每日目标快照（JSON）  
- `foods`：标准食物营养表  
- `food_logs`：用户、食物、份量、餐次、日期、来源(photo/manual/scan)  
- `recognition_tasks`：图片 url、候选结果、用户最终选择  
- `mode_configs`：后台可配的阈值模板  

---

## 6. 非功能需求

- **性能**：首页仪表盘 < 1s（缓存当日汇总）  
- **隐私**：营养数据仅本人可见；后台不展示具体吃了什么（仅聚合统计，除非后续做客服工单）  
- **合规**：慢病/孕产文案含「非医疗建议」；不做诊断、不开药  
- **可用性**：弱网可手动选食物；拍照失败有明确降级路径  

---

## 7. 里程碑

| 阶段 | 交付 | 周期（估） |
|------|------|------------|
| M0 | 技术选型、库表、微信登录、空壳小程序 | 1 周 |
| M1 | 手动选食物 + 模式目标 + 当日仪表盘 | 2 周（小程序主流程进行中） |
| M2 | 拍照识别 + 确认流 + 识别反馈后台 | 2~3 周 |
| M3 | 7 日趋势 + 管理后台食物库/模式配置 | 1~2 周 |
| V1.1 | 扫码、训练日/休息日、数据导出 | 按需 |

---

## 8. 成功指标（种子期）

- 次日留存 ≥ 35%  
- 7 日留存 ≥ 15%  
- 人均每日记录 ≥ 2 次  
- 拍照录入占比 ≥ 40%（验证 P0 假设）  
- 识别确认率（用户接受 Top1）≥ 60%  

---

## 9. 待确认事项（后续迭代记入本文档）

- [x] 技术栈：NestJS + 微信原生 + Vue3 后台 + PostgreSQL  
- [x] 食物库：自建维护，目标 800~1200 条，MVP 种子 + 后台 CRUD  
- [ ] 拍照识别供应商选型与预算（腾讯云 vs 百度）  
- [ ] 是否需「家庭成员」子账号（慢病场景）  
- [ ] 小程序类目与资质（医疗健康相关审核）  

---

## 10. 需求变更记录

| 日期 | 变更 |
|------|------|
| 2026-05-17 | 初版：五模式、拍照>手动>扫码、小程序+简单后台 |
| 2026-05-17 | 确认技术栈与食物库策略；启动 M0 工程骨架 |
| 2026-05-17 | M3：`GET /food-logs/weekly-trend`、小程序今日页 7 日趋势；后台食物/模式可编辑 |
| 2026-05-19 | 历史按日浏览（今日页日期切换）；独立「趋势」tab（7/14 天）；识别餐图本地存储 `uploads/meals` + `PUBLIC_BASE_URL` |
| 2026-05-19 | 中餐食物库种子扩充至约 119 条（`prisma/seeds/chinese-foods.ts`）；识别匹配增强（可乐鸡翅等） |
| 2026-05-19 | 食物库约 168 条；`alias-registry` + `enrichFoodSeed` 别名合并；`food-matching.util` 统一搜索/识别 |
| 2026-05-19 | 食物库扩至 **324** 条：`chinese-regional`、`fastfood-convenience`、`international-foods` + `alias-registry-ext` |
