# 手动测试清单

> 卡路里与营养分析（食光）· 适用于 M0/M1 现阶段功能  
> 自动化 API 回归见 `scripts/regression-api.ps1`（`npm run test:regression` 若已配置）  
> 测试日期：________　测试人：________　环境：□ 本机开发　□ 其他 ________

---

## 一、测试前准备（全部打勾后再测业务）

| # | 步骤 | 通过 |
|---|------|:----:|
| 1 | PostgreSQL 已启动，`apps/api/.env` 中 `DATABASE_URL` 可连库（本机 IPv6 时用 `[::1]`） | ☐ |
| 2 | 根目录已执行 `npm install` | ☐ |
| 3 | 已执行 `npm run db:migrate` 与 `npm run db:seed`（种子：五模式、admin、约 20 条食物） | ☐ |
| 4 | API 已启动：`npm run dev:api`，浏览器打开 `http://localhost:3000/api/v1/health` 返回 `status: ok` | ☐ |
| 5 | （可选）自动化 API 回归：`powershell -File scripts/regression-api.ps1` 显示 **20/20 PASSED** | ☐ |
| 6 | 管理后台：`npm run dev:admin`，可打开 `http://localhost:5173` | ☐ |
| 7 | 微信开发者工具导入目录 **`apps/miniprogram`** | ☐ |
| 8 | 开发者工具 → 详情 → 本地设置 → 勾选 **「不校验合法域名、web-view、TLS…」** | ☐ |
| 9 | 确认 `apps/miniprogram/app.js` 中 `apiBase` 为 `http://localhost:3000/api/v1`（与当前 API 一致） | ☐ |
| 10 | 建议：开发者工具 → 清缓存 → 清除数据缓存，避免旧 token 干扰 | ☐ |

**账号备忘**

| 用途 | 账号 |
|------|------|
| 管理后台 | 用户名 `admin`，密码 `admin123` |
| 小程序 | 无配置微信 AppId 时自动 **开发登录**（`dev_` + code） |

---

## 二、微信小程序

### A. 登录与引导（新用户 / 清缓存后）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| A1 | 首次打开小程序（或清除缓存后） | 能进入页面，无白屏；控制台无持续报错 | ☐ |
| A2 | 若资料未完善，应进入 **引导页** `onboarding` | 可填写性别、年龄、身高、体重、活动量、健康模式 | ☐ |
| A3 | 选择模式（如「减脂」）并提交 | 提示成功，跳转 **今日** tab | ☐ |
| A4 | 再次冷启动（资料已完善） | **不再**强制进入引导页，直接进入今日 | ☐ |

### B. 今日（首页 `pages/index`）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| B1 | 查看顶部模式标签 | 显示当前健康模式中文名（与资料一致） | ☐ |
| B2 | 查看热量进度条 / 环形或条形展示 | 有「已摄入 / 目标」或等价展示，数值合理（非 NaN） | ☐ |
| B3 | 查看蛋白 / 碳水 / 脂肪进度 | 三条宏量营养素有进度或数值 | ☐ |
| B4 | 查看达标状态文案（达标 / 注意 / 超标等） | 随摄入变化有对应文案或颜色（与当前实现一致即可） | ☐ |
| B5 | 查看「今日记录」列表 | 无记录时为空态；有记录时显示餐次、食物名、热量等 | ☐ |
| B6 | 点击悬浮 **「+」**（若有） | 能进入记录相关流程（跳转记录 tab 或录入入口） | ☐ |
| B7 | 从其他 tab 返回今日 | `onShow` 刷新后数据与最新记录一致 | ☐ |

### C. 记录（`pages/record`）— 手动选食物

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| C1 | 进入 **记录** tab，不输入关键词 | 展示默认食物列表（种子数据，约多条） | ☐ |
| C2 | 搜索「米」或「鸡」 | 列表过滤为相关食物；无结果时有合理提示 | ☐ |
| C3 | 清空搜索框 | 恢复默认列表 | ☐ |
| C4 | 点击某一食物 | 进入 **确认页** `confirm` | ☐ |
| C5 | 再次选同一食物（测最近） | 「最近使用」区域出现该食物（本地缓存，最多 8 条） | ☐ |
| C6 | 从「最近使用」点选 | 同样进入确认页 | ☐ |

### D. 确认入账（`pages/confirm`）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| D1 | 确认页展示食物名称、默认份量 | 与所选食物一致 | ☐ |
| D2 | 切换餐次（早/午/晚/加餐） | 选中态切换正常 | ☐ |
| D3 | 点击 **− / +** 调整份量（步进 10g） | 预览热量、蛋白、碳水、脂肪随份量变化 | ☐ |
| D4 | 点击 **确认记录** | Toast「已记录」，约 0.4s 后回到 **今日** tab | ☐ |
| D5 | 今日页查看 | 新记录出现在列表；总热量 / 宏量上升 | ☐ |
| D6 | 再记一条不同餐次、不同食物 | 列表两条；汇总继续累加 | ☐ |

### E. 记录 — 拍照识别

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| E1 | 点击拍照 / 相册选图 | 可调起相机或相册 | ☐ |
| E2 | 选图后 | 「AI 识别中…」Loading（最长约 60s），进入 **识别结果页** | ☐ |
| E3 | 结果页点已匹配候选 | 进入确认页，`source=photo`；确认后今日列表可见 | ☐ |
| E4 | 点「都不是，手动搜索」或未匹配项 | 回到记录页并可搜索 | ☐ |
| E5 | 点「识别有误？反馈」 | 可提交反馈并跳转搜索 | ☐ |
| E6 | 断网或停 API 后重试 | Toast 含超时/网络提示，不白屏 | ☐ |

> **说明：** 配置 `ZHIPU_API_KEY` 使用智谱 GLM 视觉；未配置时为 Mock。API 需 `npm run dev:api`。

### E2. 食物库与识别匹配

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| E2a | 记录页搜索「可乐鸡翅」 | 能搜到并可选 | ☐ |
| E2b | 拍照识别可乐鸡翅类菜品 | 结果页显示「已匹配食物库」可一键确认 | ☐ |
| E2c | 后台 Foods 列表 | 可见约 324 条（中餐菜系/便利店/日料韩餐西餐等） | ☐ |
| E2f | 搜索「寿司」「全家便当」「麦当劳」 | 能匹配对应条目 | ☐ |
| E2g | 搜索「莜面」「豆面」「窝窝头」「荞麦面」 | 能匹配粗粮类条目 | ☐ |
| E2d | 搜索别名「宫保鸡」 | 能匹配「宫保鸡丁」 | ☐ |
| E2e | 再执行 `npm run db:seed` | 同名食物别名增多、不重复插入 | ☐ |

> 维护别名：改 `prisma/seeds/alias-registry.ts` 或条目内 `aliases`，再 `npm run db:seed`

### F. 历史日期（`pages/index`）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| F0a | 点 ‹ › 切换日期 | 汇总与记录列表变为对应日期 | ☐ |
| F0b | 点击日期区域选过去某天 | 显示该日数据；不能选未来 | ☐ |
| F0c | 查看非今天 | 无悬浮「+」；可点「回到今天」 | ☐ |

### G. 趋势（`pages/trend` tab）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| G1 | 打开 **趋势** tab | 显示 7 日柱图与日均小结 | ☐ |
| G2 | 切换 **近 14 天** | 图表与范围更新 | ☐ |

### H. 我的（`pages/profile`）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| F1 | 进入 **我的** tab | 显示性别、年龄、身高、体重、当前模式等 | ☐ |
| F2 | 点击编辑资料（进入引导页 `edit=1`） | 表单预填当前数据 | ☐ |
| F3 | 修改体重或活动量并保存 | 保存成功；返回后资料已更新 | ☐ |
| F4 | 切换健康模式（picker） | Toast「已切换」；模式标签与今日目标变化（重新计算目标） | ☐ |
| F5 | 切换模式后回今日 | 目标热量/蛋白等与新模式一致（大致合理即可） | ☐ |

### G. 异常与边界（小程序）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| G1 | 停止 API，在记录页搜索 | 错误 Toast，页面不崩溃 | ☐ |
| G2 | 确认页快速连点「确认记录」 | 不重复提交多条（或仅有合理防抖；若出现 duplicate 记为缺陷） | ☐ |
| G3 | 从确认页左上角返回 | 回到记录页，未提交则无新记录 | ☐ |
| G4 | 各 tab 来回切换 5 次 | 无内存明显泄漏式卡顿、无持续报错 | ☐ |

---

## 三、管理后台（Vue）

### H. 登录与权限

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| H1 | 打开 `http://localhost:5173`，未登录访问 `/dashboard` | 重定向到登录页 | ☐ |
| H2 | 错误密码登录 | 提示失败，不进入系统 | ☐ |
| H3 | `admin` / `admin123` 登录 | 进入概览；侧栏可见菜单 | ☐ |
| H4 | 刷新页面 | 仍保持登录（`localStorage` token） | ☐ |
| H5 | 清除站点数据后访问 | 需重新登录 | ☐ |

### I. 数据概览（Dashboard）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| I1 | 进入概览 | 显示：注册用户、食物条目、总记录数、今日记录 | ☐ |
| I2 | 在小程序新增一条饮食记录后刷新概览 | **今日记录** 或 **总记录数** 增加（与库一致） | ☐ |

### J. 食物库（Foods）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| J1 | 进入食物库 | 表格列出种子食物（名称、分类、热量等） | ☐ |
| J2 | 搜索框输入「米」并触发搜索 | 列表过滤 | ☐ |
| J3 | 清空搜索 | 列表恢复 | ☐ |

> **已知限制：** 后台暂无新增/编辑表单 UI；写操作仅 API 与自动化回归覆盖。

### K. 模式配置（Modes）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| K1 | 进入模式页 | 展示五种健康模式及配置信息（当前为只读展示） | ☐ |

### L. 异常（管理后台）

| # | 操作 | 预期结果 | 通过 |
|---|------|----------|:----:|
| L1 | 登录后停止 API，切换页面 | 请求失败有合理表现（空白/报错提示），不整页崩溃 | ☐ |
| L2 | 手动删除 `localStorage` 中 `admin_token` 后点击菜单 | 跳转登录 | ☐ |

---

## 四、端到端串联（建议至少走通 1 遍）

按顺序勾选，模拟真实用户一天的使用：

| 步骤 | 说明 | 通过 |
|------|------|:----:|
| 1 | 清缓存 → 完成引导（减脂模式） | ☐ |
| 2 | 记录 tab → 搜索「米饭」→ 确认午餐 200g → 入账 | ☐ |
| 3 | 今日 tab → 确认热量与记录列表 | ☐ |
| 4 | 拍照 Mock 再记一条 | ☐ |
| 5 | 我的 → 切换「增肌」→ 回今日看目标变化 | ☐ |
| 6 | 管理后台登录 → 概览数字与小程序操作一致 | ☐ |

---

## 五、不在本次范围（测到可记缺陷，不必阻塞发布）

| 功能 | 说明 |
|------|------|
| 真实拍照识别 | M2：云视觉 + 对象存储 |
| 扫码录入 | V1.1 |
| 7 日趋势图 | M3 |
| 后台食物/模式表单 CRUD | M3（API 已有部分写接口） |
| 微信正式登录与合法域名 | 需 AppId、HTTPS 部署 |
| UI 与高保真 `design/PROTOTYPE.md` 像素级一致 | M1 后续打磨 |

---

## 六、缺陷记录

| 编号 | 模块 | 复现步骤 | 实际结果 | 严重程度 □高 □中 □低 |
|------|------|----------|----------|------------------------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

---

## 七、测试结论

| 项 | 选择 |
|----|------|
| 小程序核心流程（A–G + 第四节） | □ 通过　□ 部分通过　□ 不通过 |
| 管理后台（H–L） | □ 通过　□ 部分通过　□ 不通过 |
| 是否可进入下一阶段（M2/M3） | □ 是　□ 否，原因：________________ |

**备注：**

---

*文档随里程碑更新；API 变更时请同步修改第二节、第三节对应用例。*
